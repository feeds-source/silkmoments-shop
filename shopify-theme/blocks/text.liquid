<div class="muted">{{ block.settings.text }}</div>

{% schema %}
{
  "name": "Text",
  "settings": [
    { "type": "text", "id": "text", "label": "Text", "default": "Femme" }
  ],
  "presets": [{ "name": "Text" }]
}
{% endschema %}
